name: CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: test command
      uses: ./.github/workflows/reuse.yaml
      # run: echo "Hello Github Actions"
